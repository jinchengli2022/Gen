demo生成脚本：
创建一个生成原始demo的脚本，要求创建出的原始demo文件格式为hdf5，且结构基本如图所示。由我指定两个物体的名称与初始坐标、机械臂的初始6D位姿（4×4张量）、抓取高度（标量）、相对于待操作物体的抓取姿态（4*4张量）。机械臂移动则分为3个阶段，（1）Approch:由机械臂初始位置移动到待抓取位置（通常是操作物体正上方）（2）Grasp，执行具体抓取过程，由指定高度进入抓取姿态（3）Move抓取物体进行移动，轨迹为弧形。这三个阶段需要在subtask_info下的subtask_term_signals中体现出来，体现方式为各个阶段对应的step取值为1。同时操作物体制定也在subtask_info下的subtask_object_signals中体现出来，体现方式为操作物体的step取值为1。最后需要你将生成的demo可视化出来，并用不同颜色区分各个阶段。
把相关设置都移动带main()函数最开端方便我统一管理，同时改进几个点：1.涉及到4*4的矩阵设置，直接给我完整张量方便我直接修改 2.机械臂初始rot和grasp的rot不是同一个，是单独设置的 3.可视化的时候把机械臂的平行夹爪也示意出来

基于轨迹的数据生成：
更改并应用上轨迹生成TrajectoryGenerator类，生成规则如下：读取当前操作物体，Approch段根据操作物体新的位置得到Approch新的终点，然后根据demo中Approch段的速度（距离/轨迹点数目），线性插值出新的Approch段，保持速度一致。Grasp段同样根据物体新的位置进行齐次旋转矩阵变换，完成抓取步骤。Move段则在这时需读取操作物体的起点，也同时需要读取非操作物体的起点，根据demo中两个物体最终的相对位置，得到操作物体的新终点。对于操作物体新起点到新终点间的轨迹，pos位置采用近似于“缩放的处理”，根据xy平面上的新线段比值，对pos进行缩放，但是Z轴的最高高度需要与demo一致。并进行对应插值保持速度一致。而对于rot旋转的变化，则采用Slerp算法进行四元数插值。整体更改i流程繁杂琐碎，务必充分理解，有不清楚的地方可以问我

RLDS数据保存格式：
在你彻底理解VLA-Adapter的输入数据结构后，在data_writer.py中创建新的数据保存格式"rlds"，将生成的数据保存为类似libero中的结构，可以被VLA-Adapter按照libero原有框架读取并进入训练流程。同时在其基础上，将两个视角的渲染视频也给保存下来。同时随机挑选一次生成的数据作为检查，把| observation/image | (T, 256, 256, 3) | uint8 | 第三人称相机 RGB |
| observation/wrist_image | (T, 256, 256, 3) | uint8 | 腕部相机 RGB |
| observation/state | (T, 8) | float32 | 本体感知状态（见下文） |
| action | (T, 7) | float32 | 末端执行器增量动作（见下文） |
| language_instruction | scalar | string | 任务语言指令 |这些单独保存为hdf5格式来作为我直接检查数据保存的入口。务必仔细检查数据保存流程，有任何不清楚的及时问我。

修改数据生成数据：
我给了你两个json，有一个是libero官方数据集的dataset_info.json，他就有对数据进行分段来存储